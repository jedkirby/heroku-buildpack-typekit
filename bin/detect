#!/usr/bin/env python

import sys;

##
# NB: We cannot check whether we can use this buildpack here, as we
#     need the environment variables. This means it'll be done within the `compile`.
#
#     This works fine as it should be a non-blocking script anyway.
##
print 'Typekit'; sys.exit(0);
